/**
 * MRZ-NFC mutual authentication — placeholder module.
 *
 * The passive authentication data builder was removed during scaffolding:
 * the implementation lives in the private mobile clients (NFC session is
 * platform-specific). Re-exported types and helpers are provided by mrz.ts.
 */
export {};
